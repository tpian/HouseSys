spring:
  application:
    name: ace-gateway
  cloud:
    nacos:
      config:
        server-addr: 119.3.232.15:8848
        file-extension: yaml

    loadbalancer:
      ribbon:
        enabled: false

  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
