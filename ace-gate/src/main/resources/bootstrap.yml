spring:
  application:
    name: ace-gateway
  cloud:
    nacos:
      config:
        server-addr: xxx.xxx.xxx.xxx:xxxx #nacos地址、端口
        file-extension: yaml

    loadbalancer:
      ribbon:
        enabled: false

  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
